# OS junk
Thumbs.db
ehthumbs.db
desktop.ini
.DS_Store

# Editor
.vscode/
.idea/

# Logs
*.log

# Private knowledge file for the outreach pipeline — real GPA, transcript figures,
# passport and referee details. Attached to the Claude Project locally, never published.
pipeline/private/

# Mock/eval runs are rehearsals, not history. See mcp/outreach-server.mjs log_run.
pipeline/runs/INDEX.mock.md

# CV source and the LinkedIn copy pack. Kept OUT of the public repo: the .tex
# carries a phone number that cv.html deliberately omits, and both files quote
# private audit scores in their comments. Working copies live in
# pipeline/private/cv/, which is already gitignored.
cv/farhan-bashir-cv.tex
cv/LINKEDIN-COPY.md
